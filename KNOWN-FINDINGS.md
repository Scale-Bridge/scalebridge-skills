# Known findings (the ratchet ledger)

Append one line whenever a gate confirms a real finding that gets fixed, OR when
a reviewer flags something Jimmy accepts as intentional. Gate agents read this to
avoid re-litigating settled issues; the implementer reads it to avoid regressing them.

Format: `YYYY-MM-DD | FIXED|ACCEPTED | <one-line description> | <PR/issue #>`

<!-- entries below, newest last -->
